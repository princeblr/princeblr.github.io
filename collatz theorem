def collatz(n):
    while n > 1:
        print n
        if n % 2 == 0:
            n = n / 2
        elif n % 2 == 1:
            n = 3 * n + 1
        else:
            return "Try others"
    return n
    
    
    
    
collatz(217)
